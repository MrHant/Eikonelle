# Editor behavior clarification

Question: Should Apply update the preview's in-memory screenshot and Cancel discard the edits, with no file or clipboard output?

Decision: Yes. For now there are only in-memory updates and no saving. Apply updates the in-memory preview screenshot and Cancel discards the editing session; neither action writes a file or copies an image to the clipboard.

Resolves: editor
