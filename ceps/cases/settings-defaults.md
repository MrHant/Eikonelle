# Settings Defaults

There are default values for all settings.
Default values are applied when no settings are yet configured.

Default values are:
 * Capture hotkey: Ctrl+Shift+S
 * Capture Mode: Full Screen
