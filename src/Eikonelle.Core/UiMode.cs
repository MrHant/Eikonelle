namespace Eikonelle;

/// <summary>The appearance chosen in settings for the application UI.</summary>
public enum UiMode
{
    /// <summary>Follow the appearance Windows is configured with.</summary>
    System,

    /// <summary>Always draw the UI light.</summary>
    Light,

    /// <summary>Always draw the UI dark.</summary>
    Dark,
}
